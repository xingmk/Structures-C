#include <stdio.h>
#include <stdlib.h>

int main(int argc, char *argv[]) {
  static int k[10];
  int i, j, n, s;
  for (j = 2; j < 1000; j++) {
    n = -1;
    s = j;
    for (i = 0; i < ; i++) {
    }
  }

  return 0;
}
